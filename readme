# Job Scrapper

A simple Python script to scrape job postings from various websites.

## Requirements

- Python 3.6+
- `pip`

Install the required packages:

```bash
pip install -r requirements.txt
```

## Usage

To run the script:

```bash
python3 main.py
```
